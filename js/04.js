// Objeto Math;

let resultado;
resultado = Math.PI; // Resultado del numero PI
resultado = Math.round(2.8); // Redondea los numeros
resultado = Math.ceil(2.2); // Redondea hacia arriba
resultado = Math.floor(2.6); // Redondea hacia abajo
resultado = Math.sqrt(144);
resultado = Math.abs(-200); // Convierte el numero en valor positivo
resultado = Math.min(56, 4, 12, 9, 5, 3, 25, 2, 36);
resultado = Math.floor(Math.random() * 50); //Nos crea números aleatorios




console.log(resultado);